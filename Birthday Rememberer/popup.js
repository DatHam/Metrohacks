var addBirthdayButton = document.createElement("button");
addBirthdayButton.innerHTML = "Add Birthday";
document.body.appendChild(addBirthdayButton);

addBirthdayButton.onclick = function() {
   var addBirthdayPrompt = prompt("Enter Friend's Name", "John Smith");
   if (addBirthdayPrompt == null || person == "") {

   } else {
      
   }
};